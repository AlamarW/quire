"""quire -- shared foundations for terminal writing tools.

Import the submodules you need; nothing is re-exported here. quire deliberately knows
nothing about the apps that use it: variation is injected (callbacks, subclasses, codecs),
never toggled by a flag naming a host app.
"""
